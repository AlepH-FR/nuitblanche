{% extends 'ForumBundle::layout.html.twig' %}

{% block title %}{{ "New reply" | trans }}{% endblock %}

{% block content %}
<div class="forum  post_new" id="comment-form">
    <ul class="crumbs filters">
        <li><a href="{{ path('forum_index') }} ">{{ "Forum" | trans }}</a></li>
        <li><a href="{{ forum_urlForCategory(topic.category) }}">{{ topic.category.name }}</a></li>
        <li><a href="{{ forum_urlForTopic(topic) }}">{{ topic.subject }}</a></li>
        <li class="last">{{ "New reply" | trans }}</li>
    </ul>
    {% if not is_granted('ROLE_MEMBER') %}
    <p class="login">
        Please <strong>log in</strong> or <a href="{{ url('_secured_register') }}">register</a> in order to post comments
    </p>
    {% else %}
        <div class="main">
            <form id="admin-form" action="{{ url('forum_topic_post_create', { 'slug': topic.slug, 'categorySlug' : topic.category.slug }) }}" method="post">
                {{ form_widget(form) }}
                <button type="submit" name="reply">{{ "Add post" | trans }}</button>
                <div class="cancel">
                    <a href="{{ forum_urlForTopic(topic) }}">{{ "Back to the topic" | trans }}</a>
                </div>
            </form>
        </div>

        <div class="spacer"></div>

        {% include 'IHQSForumBundle:Post:list.html.twig' with {'posts': posts | reverse } %}
    {% endif %}
</div>
{% endblock %}