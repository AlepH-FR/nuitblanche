<div class="widget last-wars online-users">
    <h3>{{ "Online users" | trans }}</h3>
    <ul>
        {% for player in players %}
        <li>
            <a href="{{ url('player_show', { 'player_id': player.id }) }}">
                <span class="team">
                    <img src="{{ asset('bundles/ihqsnuitblanche/images/ico/player_status_' ~ player.user.status ~ '.gif') }}" alt="{{ player.user.status }}" />
                    {{ player.username }}
                </span>
            </a>
        </li>
        {% endfor %}
    </ul>
    <div class="bottom-link">
        <a href="{{ url('player_list') }}">{{ "User list"  | trans }}</a>
    </div>
</div>